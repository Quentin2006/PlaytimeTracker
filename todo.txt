1.  make delta_playtimes show up in json file. 
    DONE a. remove playtimes folder entirely
    DONE b. make tracker update playtimes and delta_playtimes directly in the json file

2.  make webpage
    DONE a. for each game in the json file create an img with the icon_url as the img
    b. add a little box under this img displaying time played today and the total playtime  
    c. make it look good (somthing like steam big picture mode)

3. make this app an exe, and make it run only off of app.py

4. make the app more performant and clean up code

5. let user select .exe they want to add

6. add some misc settings
    a. allow user to change interval timer
    b. allow user to change game picture
    c. allow user to change game name
    d. allow user to change playtime